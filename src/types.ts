export type userType = {
  fullName: string,
  email: string,
  password: string,
  phoneNumber: string,
}

export type listingType = {
  image: string,
  description: string,
  numOfBeds: string,
  numOfBaths: string,
  rating: string,
  price: string,
  userId: string
}